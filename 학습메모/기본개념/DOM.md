```java
DOM(Document Object Model)문서 객체 모델
 : 웹브라우저가 HTML 문서를 해석한 결과물
핵심개념
1. 트리 구조
 뜻: 트리 구조 HTML문서가 브라우저에 로드되면, 브라우저는 이를 트리 형태의 모델로 변환함. 각 HTML 요소는 Node(노드)가 되고, 이들이 계층적으로 연결됨.
*노드(Node)
 : 트리 구조의 각 점을 의미. HTML 요소, 속성, 텍스트 등이 모두 노드가 될 수 있음.
2. 요소 선택

```